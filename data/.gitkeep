# Mount point for the headless-engine Docker volume (docker-compose maps
# ./data -> /app/data). State files and downloads live here at runtime and
# are gitignored; this placeholder keeps the directory in the repo.
