python-telegram-bot==21.6
yt-dlp>=2025.1.15        # floor only — a fresh install always pulls the latest
python-dotenv==1.0.1
ffmpeg-downloader>=1.1   # optional: auto-downloads ffmpeg binary if not on PATH
